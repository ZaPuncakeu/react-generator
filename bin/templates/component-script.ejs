<% if(!nocss) { %>import './index.css'<% } %>

export default function <%= componentName %>() {
    return (
        <div className="<%= componentName.toLowerCase() %>">
            
        </div>
    );
}